# ───────────────────────────────────────────────────────────────
# requirements.txt  →  dependencias del bot LIVE (Docker / Linux / Windows)
# Versiones PINEADAS para reproducibilidad — actualiza intencionalmente.
# MetaTrader5 NO se incluye aquí (solo existe para Windows: ver requirements-windows.txt).
# ───────────────────────────────────────────────────────────────

# IA — OpenAI
openai==2.36.0

# Notion — Log de operaciones
notion-client==2.2.1

# Pinecone — Memoria vectorial (índice integrado llama-text-embed-v2)
pinecone==9.0.0

# Contexto externo de mercado
yfinance==1.3.0

# Utilidades
python-dotenv==1.0.1
pandas==3.0.2
numpy==2.4.4
schedule==1.2.2
requests==2.33.1
